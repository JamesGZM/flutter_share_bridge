name: share_bridge_platform_interface
description: Shared platform interfaces for Share Bridge plugins.
version: 0.1.0-dev.1
repository: https://github.com/JamesGZM/flutter_share_bridge

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.0.2
  share_bridge_core: ^0.1.0-dev.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
